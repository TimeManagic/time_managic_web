import * as React from 'react';
import * as ReactDOM from 'react-dom';

ReactDOM.render(
    <React.StrictMode>
        <p>Hello, React </p>
    </React.StrictMode>,
    document.getElementById('root')
)

